import { Meteor } from 'meteor/meteor';// Meteor.publish('characters', (text, filter)=> {//     let isOften = false;//     let output;//     if(filter == 'SHOW_OFTEN') {//         isOften = true;//     }//     if(text == '') {//         return [];//     }else {//         output = Hanzi.find({rhyme: text, isOften: isOften}, {fields: {hz: 1}, limit: 10});//     }//     if(output) {//         return output;//     }// });